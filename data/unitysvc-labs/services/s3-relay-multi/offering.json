{
  "capabilities": [
    "s3_relay"
  ],
  "description": "Multi-enrollment S3 relay. Each enrollment connects to a different upstream S3-compatible bucket. Customers provide secret names as parameters, enabling multiple storage configurations under a single account. The gateway returns presigned redirect URLs \u2014 billed per request.",
  "name": "s3-relay-multi",
  "payout_price": {
    "description": "No platform commission \u2014 customer uses own storage",
    "percentage": "100.00",
    "type": "revenue_share"
  },
  "schema": "offering_v1",
  "service_type": "content",
  "status": "ready",
  "tags": [
    "byoe",
    "gateway",
    "s3"
  ],
  "time_created": "2026-04-13T00:00:00Z",
  "upstream_access_config": {
    "s3_relay": {
      "access_key": "${ customer_secrets.{{ params.access_key_secret }} }",
      "access_method": "http",
      "bucket": "{{ params.bucket }}",
      "proxy_mode": "redirect",
      "region": "{{ params.region }}",
      "s3_endpoint": "{{ params.s3_endpoint }}",
      "secret_key": "${ customer_secrets.{{ params.secret_key_secret }} }",
      "storage_type": "s3"
    }
  }
}
