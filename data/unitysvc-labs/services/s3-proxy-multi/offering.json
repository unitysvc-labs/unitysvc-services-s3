{
  "capabilities": [
    "s3_proxy"
  ],
  "description": "Multi-enrollment S3 proxy. Each enrollment connects to a different upstream S3-compatible bucket. The gateway proxies all bytes, keeping the upstream endpoint hidden from clients. Billed per GB transferred.",
  "name": "s3-proxy-multi",
  "payout_price": {
    "description": "No platform commission \u2014 customer uses own storage",
    "percentage": "100.00",
    "type": "revenue_share"
  },
  "schema": "offering_v1",
  "service_type": "content",
  "status": "ready",
  "tags": [
    "byoe",
    "gateway",
    "s3"
  ],
  "time_created": "2026-04-13T00:00:00Z",
  "upstream_access_config": {
    "s3_proxy": {
      "access_key": "${ customer_secrets.{{ params.access_key_secret }} }",
      "access_method": "http",
      "bucket": "{{ params.bucket }}",
      "proxy_mode": "proxy",
      "region": "{{ params.region }}",
      "s3_endpoint": "{{ params.s3_endpoint }}",
      "secret_key": "${ customer_secrets.{{ params.secret_key_secret }} }",
      "storage_type": "s3"
    }
  }
}
